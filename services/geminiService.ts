
/** 
 * IA Desativada pelo usuário. 
 * Este arquivo não realiza mais chamadas ao Google Gemini.
 */
export const generateEventBackground = async (prompt: string): Promise<string> => {
  throw new Error("Geração de imagem por IA está desativada.");
};
