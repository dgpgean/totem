
{
  "name": "totem-genius-photo-booth",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p .vercel/output/static && cp -r . .vercel/output/static 2>/dev/null || true"
  }
}
